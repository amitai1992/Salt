import { ApiDataCategories } from "../models"

export const columns = ["","Name", "PII", "Masking", "Type"]

export const categoriesTrans:ApiDataCategories = {
    urlParams:"url parameters",
    queryParams: "query parametrs",
    headers:"headers",
    body:"body"
}


